Alpine.data('detailsTicket', () => {
    return {
        displayCalendarMode: 'calendar',
        urlDisplayImg: '',

        init() {
            //
        },

        openImage(url) {
            this.urlDisplayImg = url;
        },
    };
});
